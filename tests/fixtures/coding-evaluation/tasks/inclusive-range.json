{
  "id": "inclusive-range",
  "title": "Repair inclusive range boundaries",
  "cwd": ".",
  "prompt": "Fix range.mjs. range(start, end, step) must include end when it falls on a step, return an empty array when start is greater than end, and throw RangeError for a nonpositive step. Preserve the exported API.",
  "files": {
    "range.mjs": "export function range(start, end, step = 1) {\n  if (step <= 0) throw new RangeError('step must be positive');\n  const values = [];\n  for (let value = start; value < end; value += step) values.push(value);\n  return values;\n}\n"
  },
  "immutable": [],
  "checks": [
    { "id": "inclusive-end", "kind": "export", "path": "range.mjs", "name": "range", "args": [2, 4], "expected": [2, 3, 4] },
    { "id": "single-value", "kind": "export", "path": "range.mjs", "name": "range", "args": [5, 5], "expected": [5] },
    { "id": "stepped", "kind": "export", "path": "range.mjs", "name": "range", "args": [-2, 4, 2], "expected": [-2, 0, 2, 4] },
    { "id": "empty-range", "kind": "export", "path": "range.mjs", "name": "range", "args": [4, 2], "expected": [] },
    { "id": "invalid-step", "kind": "export", "path": "range.mjs", "name": "range", "args": [1, 3, 0], "throws": "RangeError" }
  ]
}
