# mycli shell adapters

This folder is the boundary between promoted terminal UI components and mycli runtime data.
UI source must not import another agent runtime directly when promoted into the compiled shell.
