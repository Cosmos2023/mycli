# mycli shell adapters

This folder is the boundary between copied coding-agent UI components and mycli runtime data.
Copied UI source must not import the original agent runtime directly when promoted into the compiled shell.
