# imported-ui

This directory is a direct copied source pool from the reference coding-agent interactive UI, renamed for mycli ownership.

Rules:
- Do not import these files into the compiled shell until their runtime dependencies are adapted.
- When promoting a component, preserve the reference component behavior and only replace data/runtime dependencies with mycli adapters.
- The compiled mycli shell lives outside this directory.
