# Doctor Tool Environment Notes

Existing doctor checks:

- config
- logs and logs redaction
- traces and tool execution diagnostics
- tool manifest
- TUI/runtime/MCP placeholders

Target addition:

- inspect `SHELL`/fallback shell path
- inspect `git` via injected `which`
- do not execute commands or create files
